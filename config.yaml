model_name: "Qwen/Qwen2.5-1.5B-Instruct"
device: "cpu"

num_epochs: 1  # Just 1 epoch
batch_size: 1
learning_rate: 5.0e-7
gradient_accumulation_steps: 4
max_grad_norm: 0.5

num_samples_per_question: 1  # Reduced!
max_new_tokens: 128  # Reduced!
temperature: 0.7
top_p: 0.9

beta: 0.3

train_data: "data/train.jsonl"
eval_data: "data/math500.jsonl"
output_dir: "checkpoints/"
log_dir: "logs/"

eval_batch_size: 1
eval_num_samples: 1